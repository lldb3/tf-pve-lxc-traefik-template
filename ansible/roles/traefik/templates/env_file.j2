### Data
DATADIR="{{ trackstack_dir }}/data"
CONFIGDIR="{{ trackstack_dir }}/config"
STORAGEDIR="{{ trackstack_dir }}"

TZ="{{ trackstack_timezone }}"
PUID="{{ trackstack_dir_owner }}"
PGID="{{ trackstack_dir_group }}"

DOMAINNAME={{ traefik_domain_name }}
CERT_POSTMASTER_MAIL={{  cert_postmaster_name }}

# watchtower
PUSHOVER_URL="{{ pushover_url }}"
HOSTNAME={{ hostname }}
